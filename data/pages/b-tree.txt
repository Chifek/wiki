====== B-Tree ======


  * Элемент ненумерованного спискаСоздается по умолчанию ''CREATE INDEX index_name ON table_name(column_name)''

  * Элемент ненумерованного спискаПоддерживает операции: ''<, >, <=, >=, =''

  * Элемент ненумерованного спискаПоддерживает LIKE 'acb%' (но не '%abc')